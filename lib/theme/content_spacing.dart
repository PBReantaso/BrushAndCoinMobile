/// Space between the bottom of [BcAppBar] and the start of screen body content.
/// (Does not change the app bar’s own height—only the body inset.)
const double kContentBelowAppBarPadding = 14;

/// Horizontal inset for main screen body content (below the app bar, above the bottom nav).
/// Does not apply to [BcAppBar] or the bottom [NavigationBar] — use on [Scaffold.body] padding.
const double kScreenHorizontalPadding = 15;
